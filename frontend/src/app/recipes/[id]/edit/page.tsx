export default function EditRecipePage({
    params,
}: {
    params: { id: string };
}) {
    return (
        <div className="container mx-auto py-8">
            <h1 className="text-3xl font-bold mb-6">Edit Recipe</h1>
            {/* We'll add the RecipeForm component here later */}
        </div>
    );
}